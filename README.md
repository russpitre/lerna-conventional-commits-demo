# lerna-conventional-commits-demo
A project to demo using lerna with conventional commits


On the develop branch:

```shell
lerna version --yes --conventional-commits --conventional-prerelease --preid dev
```

